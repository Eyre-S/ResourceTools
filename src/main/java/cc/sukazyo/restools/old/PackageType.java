package cc.sukazyo.restools.old;

/**
 * The two different storing types of package files.
 * DIR: in a directory that exists in the disk.
 * JAR: in a packaged jar.
 */
public enum PackageType {
	DIR,
	JAR
}
